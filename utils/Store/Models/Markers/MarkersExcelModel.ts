export interface MarkersExcelModel {
    strada: string,
    numar: number,
    latitudine: string,
    longitudine: string,
    stalp: string | null,
    lampa: string | null,
    tip: string | null,
    observatii: string | null,
    putere: string | null,
    hub_c: string
}